import React, { FC } from 'react';

interface CardProps {
    coinCode: string;
    subtitle: string;
  }

  const alertCard: FC<CardProps> = ({ coinCode, subtitle }) => {
    return (
        <>
        </>
    )
  }

  export default alertCard;